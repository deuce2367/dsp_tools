// test_main.cpp
// Since we are linking against Catch2::Catch2WithMain, we just need a dummy translation unit if we want,
// or we can omit this file. However, adding it ensures CMake compiles it properly if we ever need custom main.

// Empty - Catch2WithMain provides the main()
